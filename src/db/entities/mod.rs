// The applications table lands in phase 4; the entity exists now so the schema
// migration and entity set stay in one place.
#[allow(dead_code)]
pub mod application;
pub mod company;
pub mod posting;
pub mod prelude;
